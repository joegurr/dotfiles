- clean: ['~']

- link:
    ~/.vimrc: vimrc
    ~/.tmux.conf: tmux.conf
    ~/.bashrc: bashrc
    ~/.profile: profile
    ~/.gitignore: gitignore
    ~/.bash_aliases: bash_aliases
    ~/.bash_python_config: bash_python_config
