- clean: ['~']

- link:
    ~/.vimrc: vimrc
    ~/.tmux.conf: tmux.conf
    ~/.bashrc: bashrc
    ~/.profile: profile
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
    ~/.config/i3/config: i3config
